# Required for OpenRouter datasets (rankings-daily, app-rankings). Any inference key works.
OPENROUTER_API_KEY=
# Optional. Cloudflare Radar read token (Account > API Tokens > "Radar" template).
CF_API_TOKEN=
# Optional. Artificial Analysis direct API (not needed: OR /models already carries AA indices).
AA_API_KEY=
